import { contentContainer, pageSubtitle, pageTitle, studioShell } from '@/shared/ui/recipes';

export const recordPageShell = studioShell;

export const recordContentContainer = contentContainer;

export const recordPageTitle = pageTitle;

export const recordPageSubtitle = pageSubtitle;
